(function () {
    'use strict'

    function RegisterCtrl($scope, $http, $route, globalConstants) {
         
    };

    angular.module('app')
        .controller('RegisterCtrl', ['$scope', RegisterCtrl]);
} ());